PRODUCT_BUNDLE_IDENTIFIER = com.myexample.prod
BUNDLE_ID = $(PRODUCT_BUNDLE_IDENTIFIER)
CODE_SIGN_IDENTITY=iPhone Distribution
PROVISIONING_PROFILE_SPECIFIER = match AppStore com.myexample.prod
PROVISIONING_PROFILE = $(sigh_${BUNDLE_ID}_appstore)
DEVELOPMENT_TEAM = XXXXXXXX

// For Firebase Analytics
OTHER_LDFLAGS = $(inherited) -ObjC
